mlflow_params:
  run_name: 'new_seed'

pipeline_params:
  test_size: 0.25
  random_state: 123

model_params:
  n_estimators: 200
  max_depth: 8
  min_samples_leaf: 1
  max_features: 'auto'
  random_state: 42